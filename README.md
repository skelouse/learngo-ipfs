Just started with go, I belive these should be run as their own package.

EX:

```
$ cd api_interaction
$ go run main.go
```

```
$ cd ..
$ cd building_api
$ go run main.go
```

- Not started:
```
$ cd ..
$ cd frontend_for_api
$ go run main.go
```